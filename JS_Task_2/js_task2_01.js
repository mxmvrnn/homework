function typeCheck (a){ 
    if (typeof a=="number") 
    console.log(a +': Вы ыыели число'); 
    else if(typeof a=="string") 
    console.log(a+': Вы ввели строку'); 
    else 
    console.log(a+' Это не число и не строка '); 
    }; 
    
    typeCheck('кекмакек');
    // вариант 1, поиграть с возвращаемым знаечением


function
console.log('This is ' +typeof(name));
// вариант 2, сразу использовать возвращаемое значение
 
